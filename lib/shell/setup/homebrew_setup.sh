#!/usr/bin/env bash

# External Script:

# [[file:../../../workstation.org::*set up homebrew (macos)][set up homebrew (macos):2]]
function homebrew_setup() {
    /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
}
homebrew_setup
# set up homebrew (macos):2 ends here
